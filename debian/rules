#!/usr/bin/make -f

# export DH_VERBOSE = 1
export PYBUILD_NAME = virtualbricks
export PYBUILD_DISABLE = test

%:
	dh $@ --with=python3 --buildsystem=pybuild

DEB_INSTALL_MANPAGES_virtualbricks = debian/virtualbricks.1
